<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Myspace</title>
    <link rel="icon" href="https://maxcdn.icons8.com/Share/icon/p1em/Logos//myspace1600.png">
    <link rel="stylesheet" type="text/css" href="/css/default.css">
  </head>
  <body>
    <% if flash[:notice] %>
      <div class="flash notice">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="flash alert">
        <%= flash[:alert] %>
      </div>
    <% end %>
    <% unless @current_user %>
    <div class="topblue">
      <form class="signinform" action="/sign-in" method="post">
        <label>Sign In</label>
        <input type="text" name="username" placeholder="username">
        <input type="password" name="password" placeholder="password">
        <input type="submit" >
      </form>
    </div>
    <h1>Hello</h1>
    <% else %>
    <div class="topblue">
      <form class="signinform">
      <label><%= @current_user.username %></label><a href="/logout"><button type="button" name="button">Logout</button></a>
      </form>
      <img src="http://blog.visme.co/wp-content/uploads/2015/10/color1.jpg">
    </div>
    <div class="navbar">
      <p>Home | Browse | Search | Invite | Film | Mail | Blog | Favorites | Forum | Groups | Events | Videos | Music | Classifieds</p>
    </div>
    <% end %>
      <%= yield %>
  </body>
</html>
